{
  "name": "docker-sign-test",
  "version": "1.0.0",
  "description": "A repo which signs a docker image with CI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
